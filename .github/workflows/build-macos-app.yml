name: Build macOS App

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pyinstaller

      - name: Build .app bundle
        run: |
          pyinstaller --clean --noconfirm --windowed --name TJR --osx-bundle-identifier com.tjr.desktop --paths src src/tjr/__main__.py

      - name: Package .app to zip
        run: |
          ditto -c -k --sequesterRsrc --keepParent "dist/TJR.app" "dist/TJR-macOS.zip"

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: TJR-macOS-app
          path: dist/TJR-macOS.zip
          retention-days: 14

      - name: Upload macOS quick start guide
        uses: actions/upload-artifact@v4
        with:
          name: TJR-macOS-quick-start
          path: docs/TJR_MACOS_QUICK_START_RU.md
          retention-days: 14

      - name: Publish GitHub Release (tag builds)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          name: TJR ${{ github.ref_name }}
          generate_release_notes: true
          overwrite_files: true
          files: |
            dist/TJR-macOS.zip
            docs/TJR_MACOS_QUICK_START_RU.md
